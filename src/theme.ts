//	Mantine theme. Intentionally empty for now -- fill this in (fonts, colors,
//	component defaults) as the game's UI moves over to Mantine components.
import { createTheme } from "@mantine/core";

export const theme = createTheme({
  fontFamily: "Capriola, sans-serif",
});
